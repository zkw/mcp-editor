# AGENTS.md

本项目用于实现基于锚点补全（anchor-completion）的 MCP 编辑服务。

- 使用 `bun pm pack --filename mcp-anchor-shell-0.1.0.tgz` 生成 tarball。
- 安装时若遇到相对路径解析错误，改用绝对路径进行 `bun install -g /full/path/mcp-anchor-shell-0.1.0.tgz`。
- 确保打包后包文件存在并且路径正确。
